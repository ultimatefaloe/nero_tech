1. Build a Student Registration Form with these fields:
- Full Name (text, required)
- Email (email type, required)
- Phone Number (tel type)
- Gender (radio buttons: Male / Female)
- Course (dropdown: HTML, CSS, JavaScript)
- Date of Birth (date type)
- Message/Bio (textarea)
- Submit button
2. Add required validation to Name, Email, and Gender.
3. Group Personal Info and Course Info into separate fieldsets.



1. Create a new file called styles.css in your project folder.
2. Link it to your index.html using <link>.
3. Set the body background-color to a light grey (#f5f5f5).
4. Set all h1 elements to use color: navy and font-size: 40px.
5. Set all paragraphs to font-size: 18px and line-height: 1.6.


1. Create 5 paragraphs. Give 3 of them class="featured".
2. Use a class selector to make featured paragraphs have a blue left border.
3. Give one paragraph id="special" and make it have a gold background.
4. Use the universal selector to remove all default margin and padding.
5. BONUS: Use an attribute selector to style all required form inputs.


1. Create a hero section div with a full-width background image.
2. Set background-size: cover and background-position: center.
3. Create a footer with a dark linear gradient background.
4. Style headings with letter-spacing and text-transform: uppercase.
5. Add text-shadow to your h1.

1. Build a navigation bar using Flexbox.
- Logo on the left, nav links on the right.
- Use justify-content: space-between.
2. Build a 3-column card layout using CSS Grid.
- Each card has an image, title, and description.
- Use gap: 24px between cards.
3. BONUS: Make the grid responsive — 1 column on mobile, 3 on desktop.


1. Build a full webpage with:
- A fixed navbar with logo and links
- A hero section with a gradient background and centered text
- A 3-column services section using CSS Grid
- A contact form
- A footer
PageFull Stack Web Development — Student Textbook
2. Use CSS Variables for all your colours.
3. Add hover transitions to all buttons and links.
4. Make the layout responsive using media queries.